#include "main.h"
/**
 * _abs - absolute value
 * @n: integer
 *
 * Return: return absolute valuer of a number
 */
int _abs(int v)
{
	if (v < 0)
	{
		v = v * -1;
		return (v);
	}
	else
	{
		return (v);
	}
}
