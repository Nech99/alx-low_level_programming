#include "main.h"
/**
 * _abs - absolute value
 * @v: integer
 *
 * Return: return absolute value of a number
 */
int _abs(int v)
{
	if (v < 0)
	{
		v = v * -1;
		return (v);
	}
	else
	{
		return (v);
	}
}
